% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/feat_calendar.R
\name{feat_calendar_dt}
\alias{feat_calendar_dt}
\title{Add Calendar Features}
\usage{
feat_calendar_dt(
  df,
  date,
  dow = TRUE,
  woy = FALSE,
  month = TRUE,
  eom = TRUE,
  dom = FALSE,
  hod = FALSE,
  moh = FALSE
)
}
\arguments{
\item{df}{A data frame containing time series data}

\item{date}{Symbol or character naming the date column (Date or POSIXct class)}

\item{dow}{Logical, add day of week as ordered factor (Monday-Sunday)}

\item{woy}{Logical, add week of year (1-53)}

\item{month}{Logical, add month as factor (01-12)}

\item{eom}{Logical, add end-of-month indicator (0 or 1)}

\item{dom}{Logical, add day of month (1-31)}

\item{hod}{Logical, add hour of day (0-23) - requires POSIXct}

\item{moh}{Logical, add minute of hour (0-59) - requires POSIXct}
}
\value{
Data frame with calendar features added
}
\description{
Add calendar-based features such as day of week, month, week of year, etc.
These features are commonly useful for capturing seasonal patterns in time series.
}
\examples{
\dontrun{
# Add default calendar features (dow, month, eom)
df_cal <- feat_calendar_dt(df, date = date)

# Add all calendar features
df_cal <- feat_calendar_dt(df, date = date,
                            dow = TRUE, woy = TRUE, month = TRUE,
                            eom = TRUE, dom = TRUE)

# Add sub-daily features for POSIXct data
df_cal <- feat_calendar_dt(df, date = datetime, hod = TRUE, moh = TRUE)
}
}
