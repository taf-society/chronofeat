% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit.R
\name{as_model_spec}
\alias{as_model_spec}
\title{Convert Model Function to Model Specification}
\usage{
as_model_spec(model_fn, ...)
}
\arguments{
\item{model_fn}{A model function that accepts a formula and data argument}

\item{...}{Additional arguments passed to the model function during fitting}
}
\value{
A list with \code{fit} and \code{predict} functions:
\itemize{
\item \code{fit(y, X, ...)} - Fits the model with response y and predictors X
\item \code{predict(object, newdata, ...)} - Generates predictions from fitted model
}
}
\description{
Converts a model function (e.g., \code{lm}, \code{glm}) to a model specification list
with \code{fit} and \code{predict} functions compatible with the chronofeat API.
}
\details{
This function provides backward compatibility for users who want to pass
model functions directly (e.g., \code{model = lm}) instead of model specifications.

The wrapper creates a formula-based interface internally, combining y and X
into a temporary data frame and fitting the model using the standard R formula
interface.

Supported model functions include any that accept the standard formula/data
interface: \code{lm}, \code{glm}, \code{randomForest::randomForest}, \code{ranger::ranger}, etc.
}
\examples{
\dontrun{
# Convert lm to model spec
lm_spec <- as_model_spec(lm)

# Convert glm with family argument
glm_spec <- as_model_spec(glm, family = poisson())

# Use directly in fit()
m <- fit(value ~ p(12), data = df, date = "date", model = lm)
}

}
