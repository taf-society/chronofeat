% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/feat_trend.R
\name{feat_trend}
\alias{feat_trend}
\title{Add Trend Features}
\usage{
feat_trend(df, date, groups = NULL, degrees = 1L)
}
\arguments{
\item{df}{A data frame containing time series data}

\item{date}{Symbol or character naming the date column (used for sorting)}

\item{groups}{Character vector naming grouping columns}

\item{degrees}{Integer vector of polynomial degrees (e.g., c(1, 2) for linear and quadratic)}
}
\value{
Data frame with trend features added
}
\description{
Generate polynomial trend features for time series data. Trends are computed
as sequential indices within each group, with optional polynomial transformations.
}
\details{
Trend features are sequential indices (1, 2, 3, ...) within each group.
Polynomial degrees allow capturing non-linear trends:
\itemize{
\item degree 1: linear trend (1, 2, 3, ...)
\item degree 2: quadratic trend (1, 4, 9, ...)
\item degree 3: cubic trend (1, 8, 27, ...)
}
}
\examples{
\dontrun{
# Add linear trend
df_trend <- feat_trend(df, date = date, groups = "store_id", degrees = 1)

# Add linear and quadratic trends
df_trend <- feat_trend(df, date = date, groups = "store_id", degrees = c(1, 2))
}
}
